import { BACKGROUND_COLORS } from '@/design/colors'
import { SPACING } from '@/design/spacing'

export const DividerWrapper = styled.div`
  background: ${BACKGROUND_COLORS.two};
  width: 2px;
  height: inherit;
  margin: 0 ${SPACING.md}px;
`
